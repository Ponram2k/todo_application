spring:
  application:
    name: todo_application
  datasource:
    url: ${DB_URL} #jdbc:postgresql://localhost:5432/todo_db
    username: ${DB_USERNAME} #Application
    password: ${DB_PASSWORD} #20190506
#    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    generate-ddl: true
#    defer-datasource-initialization: true
  hibernate:
    ddl-auto: update
#  sql:
#    init:
#      mode: always
#      platform: postgres
#  properties:
#    hibernate:
#      dialect: org.hibernate.dialect.PostgreSQLDialect
server:
  servlet:
    context-path: /todo
  port: 9090